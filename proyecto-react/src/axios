axios.get('http://miapi.com/datos')
  .then(response => {
    // Manejo de la respuesta exitosa
    console.log(response.data);
  })
  .catch(error => {
    // Manejo del error en la solicitud
    console.error('Hubo un error al hacer la solicitud', error);
  });
